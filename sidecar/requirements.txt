# Pinned runtime dependencies for the HTTP sidecar. The Dockerfile
# installs the same versions; keep both in sync. Run the sidecar
# locally with:
#
#   pip install -r sidecar/requirements.txt
#   cd sidecar && OPENHOST_APP_DATA_DIR=/tmp/gemini-data \
#       python3 -m uvicorn server:app --port 8080
starlette==0.41.3
uvicorn[standard]==0.32.1
